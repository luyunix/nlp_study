# 迁移学习与Transformers任务原声逐段记录：P175

> 本文直接由视频音轨识别并按约一分钟分段，不使用站内字幕。
> 它用于核查老师讲解是否遗漏；口误、识别错误和重复表达会在对应学习文章中进一步校正。

## 00:00–00:55

好,来吧,接下来来玩最后一段模型皮肤相关的内容。这个其实跟之前的套路其实都是差不多的,所以我们就直接写了,土多久,这里面就是模型皮肤函数。皮肤函数它的流程就是,首先,第一个就是,加上差不多加载数据。接下来第二个动作就是,加载什么?或者说叫创建加载器。就是你的数据加载器,然后加载模型。然后完成以后叫P量、P量预测。接下来计算准确率,然后下往下边有操作叫打印结果。所以刚才在这里面的其实我们算的是比如说轮数,这是迭代部署920到940。所以这里面打的损失包括ECC是920到940中间,这20P它的结果,大家看这就行了。

## 00:55–01:52

那么有人会说老师,那你这样的话,你所时跟准确率好像也不是特别准,它准确率到底是多少。咱们在模型评估这里面做个具体的运算就可以了。好了,那于是上来以后就是DefandEVLUATEVALUATEVModel我们直接写了。第一步操作就是加载什么级,加载数据级,这里边写下叫测试。来于是这个动作叫Tanse的BetaSight等于谁呢?等于Lode这堆。那注意啊,我们换的是Tanse的点CSV,这个地方不要写错了。Split这个地方你可以依旧写上去,无所谓的。大家说老师,Split的切割我要不要一张刺你切割,没必要。因为我们在这里面是不是已经对你的文件数据,就是对文件数据已经做切割了。所以你没必要在这个文件中再切割一次,所以直接写去就行了。好,那么玩整个文件。

## 01:52–02:51

那接下来再往下边就是第二步叫创建,刚才我说了第一步是创建数据加载数据啊。第二步是创建加载器,那于是这个动作就是创建加载器,加载器。加载器跟像,其实就是数据加载器。My,My怎么?My data loader, data loader等于谁呢?等于又是那套,叫ganon data loader,但是不好意思,你不能这么写。因为按住ctrl不松手,切到ganon data loader这,你会发现,呀,这个函数中,它玩的居然是一个,帮我念一下是哪个路径啊。是不是trainers sb那个路径啊?你要想让这个函数是通用的,你就得干一件事。干一件啥事,把这个文件路径干嘛?载到上面,就到上面,这是第一个操作。第二个操作就是,我们可以选safo等于chord和foss,把chord和foss也传上去,其他的一模一样。

## 02:51–03:49

能听清楚我再说啥吧?所以My data loader,你得把里边那套给它复制过来。里边那套复制过来以后,你不能掉这个函数啊,不能掉,不能掉它,你得data loader自己写。第一个动作,你得改两地儿啊。data loader里边改两地儿。第一个动作就是,把你的data set从之前的train给它换成什么?太似的,所以这是输入,然后输入其对象。第二个动作,8p这一堆都没问题。safo等于这个操作,chord这个操作其实应该,正常来讲应该得换成什么?foss,你在评估的时候,正常就不需要再去打乱你们的数据了。更上次路了吧?接下来,往下再来。下边这一堆呢,也就是你正常的做处理了。那你告诉我,joblast,joblast是啥意思?是不是删除最后一批啊?这个操作你可以玩的里不正,就是这一坨。来吧,跟着我的孙悟,那开始来了。第三个动作就是,不用再训练了,那就直接加载。

## 03:49–04:48

叫加载训练好的模型。好,那么来,开始吧。pass,pass和运,来吧,路径给我搞一个,搞哪个路径?点gaum,model gaum,gaum谁?classification,123,选一个啊。选哪一个?肯定选三嘛。好,那接下来再往下边来。第二个动作就是叫mymodel,mymodel等于谁?ammodel,todevice,点todevice。好,dvse,然后接下来有个操作就是mymodel,mymodel要加载模型参数,有个叫loadstate,还记得这个参数吗?扣几点load下载,好,就是加在训练好的模型参数。好,接下来我们开始来写第四个动作。第四个动作就是该干嘛啊?初始化屏幕参数。那第一个操作就是叫correctree,correctcorrectcorrect

## 04:48–05:48

correct第二个是toto,然后等于谁呢?零和零,他们分别记录啥呢?第一个就是预测正确的一个样本数,还有预测的总样本数。你一会儿算你的一个准讯率的时候,你是不是拿着correct,处于你的toto是不是就可以了?好,来啦,接下来设置还有操作,第五个,叫设置模型为屏幕模式。好,那就是你的model.evo就可以了。来啦,开始了,第六个叫列带预测。列带预测,你要每p4都去测一下,叫通过数据加载器,然后或许每p4数据,然后干嘛呢?预测统计即可,来啦,开始写了for i,做画又是那个小画,跟这一样。in the new model,你 new model in the model,你里边写上谁呢?my.loader,这里边加一个东西,还记得是谁吗?tto.m,加上以后写一句话,4大个等于1,是不是p4从一开始啊?

## 05:48–06:44

就是dp,d2p,加好看。然后里边写一套,会了吧?那四个参数,还记得吗?input,inds,token,tapa,inds,然后attention,mask,和lables,这一堆。那接下来,我们开始来做,第一个动作,6.1,干嘛呀?叫将,将参数,将将,将参数移动到,移动到哪里?tpu,第1个,第2个,第3个,第4个。都移动完毕以后,6.2,干嘛?叫计算还是不计算tpu?不计算tpu,不计算tpu怎么写?刚才说我vase,叫做点no grid。能不能跟上来?预测的时候是不计算tpu的,然后接下来开始来,6.3。说6.3干啥?前枪传播。

## 06:49–07:48

输入数据,给模型,然后后续结果。那于是就是output等于,买model,把那几个参数拉过来就行了,然后6.4。或许,预测结果,那这是最大的改率,tap等于谁?tap等于today2.baps,把你刚才的结果,找到改率最大的那个就行了,那6.5,统计预测正确的样本数,那就是zoa,ecd,correct,加等于谁?这个上个视频刚写过,tap等于lables,然后做一件事,sum,dare,item,这是不是预测正确的?接下来6.6,统计统样本。统样本是不是直接加label就行了?6.7,干啥了?说出预测进度,说出预测进度,

## 07:48–08:48

接下来,接下来,20步打印一下,打印一次,那就是e4干啥?i跟谁?20区都对定,那我做一件事,打印6.7.1,打印什么?平均准确率,那于是就是扶着停一下,f,f里边写上,叫什么?acc,acc是不是在了一个准确率,准确率写上,来保守怎么做?是不是另个门处一堂,然后来个drf,那接下来,下个动作就该,解码,第一个样本的文本,解码,第一个样本的文本,然后呢,并打印预测结果,咱看一条数据,我们来看看,首先taps的list等于谁呢?等于mytokenider,可以把你的持续,那个,持的一个编号,硬射成你的内容,

## 08:48–09:46

那所以这个操作是一个,叫dcoder,这个很出,dcoder里边写啥呢?input ids,input ids,你告诉我,这啥意思?0,这是不咱们去pc里边找一条样本啊,还记得这个操作吗?special token等于2,然后还有一个clear,coken,coken,coken,coken,coken,coken,coken,coken,coken,coken,是Tan's的歷史的操作那我來告訴我預測的標籤還是ProtentF來了叫預測的一個標籤是標籤是誰碳盤中框號中二什麼0

## 09:46–10:45

然後真實標籤真實標籤是哎寫外表真實標籤是誰Label's 0也就是說你在這裡邊其實這方面待會兒可以不寫就這兒可以不寫因為在這裡邊你每20p在這裡邊打印一下你的準確率就可以了後面一個動作可以不寫但是在這裡邊我解碼第一個樣本解碼第一個樣本是啥意思是這20p4裡面它的第一個樣本我在這裡邊解碼一下解碼是啥意思呢我通過MyTomeNider基於你剛才的八條數據的第一條我把你重新印色成文本我給你打出來然後我們看看預測標籤和真實標籤就是到底這個文本內容咱自己判斷一下它是一個好評還是一個差評所以我再說一遍其實我先再說一遍這個內容你可以不寫然後只看上半個準確率就行了那既然我們把下半個動作給它出示了

## 10:45–11:44

出示以後第五個動作就是咱的測試以外留言報導來咱玩一下咱玩一下右鍵走好現在模型我們在這裡邊評估的一個動作已經啟動了其實非常簡單就是在這裡邊搞一個下載器拿到你的數據拿到你數據以後接下來在這裡邊做一件事三四設置模型然後這裡邊一條一條做就行了來吧你看這孩挺快的稍等一下8990多100然後結束然後這裡邊你來看整個結果就是準確率大概是在多少來的告訴我0.870.880.8880多接近90其實還不錯然後你看看這句話你告訴我這是啥意思原始文本是買了段時間了一直都在用一直都在用很不錯用什麼玩意兒是很不錯然後用盤重新做的新系統機子運行很快也很穩定

## 11:44–12:44

好評吧預測是1老正式標籤也是個1然後你看這這也是預測對了這也是預測對了這也是預測對了這也是預測對了然後你會發現一件事每p4的第一條都出對了那有人有說老師我不想看這批的第一個樣本怎麼辦你寫個循環把這個0這個玩意兒換成i然後把這個動作全解析了就行了能看明白吧所以就是每p4出一條樣本讓咱看看這個動作預測到底是OK然後這活MITOPHONIDAR的DQ是啥是解碼解碼是啥意思解碼是把你這裡面的內容重新給它映射成文本那我再問你個問題你還記得input的IDS裡面放的是啥嗎放的是那個文本對應的詞彙表中的編號就是那個所以嘛對不對好了叫詞句編號行這個內容就講到這一p4看一條就行了你要想看所有就佛循環

## 12:44–13:43

再打印一下然後看所有就OK來吧接下來咱總結一下首先中文文本和類我們就暫時玩到這最終就是我們那個結果預測和真實是多少然後這個準確率是多少這一堆來吧簡單說一下中文文本和類Data set數據級處理的流程你做了一件什麼事數據級做了啥事先通過load data set是不是去加載數據加載數據有兩種方式加載完畢後然後按照每p4做處理有階段有補習有最大廠度最後呢返回四個結果分別是第一個持續編號就音譜來DS第二個句子類型就是talk and type第三個是研碼注意力研碼那個attention mask最後label是你的真實標籤好那麼他說的是這一坨好那接下來來他是如何進行二的進行遷移的大家有沒有發現一件事

## 13:43–14:43

我們是咋做的我們自己寫的模型其實就一行代碼大家知道是哪一行嗎是不是那個那個把人家處理後的768倍轉成一個幾倍的二倍的數據後面結合一個soul of the max應設成概率什麼就可以了好了那有人說老師有一個小細節剛才課題有同學提問問了一個小細節大家看這你能解釋一下嗎在這裡邊來你告訴告訴我我用的所是喊說是哪個crossnchofeloss然後呢那有人就快在這糾結了老師不對你做的是一個二分類的問題二分類不是說要用bce嗎我是說建議用bce我沒說百分之百要用它二分類我們可以用二分類bce交加商多分類我們可以用那個cross nchofeloss但是二分類問題我能不能用cross nchofeloss從思路來做可不可以呢可以那有人說老師我就想用bce

## 14:43–15:38

你換一下就是就因為他代碼的事無所謂你可以換一個損失函數然後這裡邊把它換成bce但還有一個問題如果我將來做的不是二分類是三分類或者四分類或者別的那我的損失函數是不是固定就不用動了你要調的其實就是你的哪一個搭建模型證你的這個層如果三分類你這兒將來寫三四分類這裡邊寫四是不是可以啊而這個動作所以這個細節大家要注意好了那接下來再往下邊我們接著看所以他的動作就是先用預訓練模型得到結果在後邊聽起來一個sofmax進行一個二分類就可以了好那後邊有一個來看看這個程序吧ab程序分別代表啥意思能看明白嗎你看這篇叫vce 套起來能活著的這是啥意思這是不是後更新參數啊a在這裏邊是誰的動作a是誰的動作告訴我a是誰的動作

## 15:48–16:48

a是誰的動作a是Burnt模型處理後的結果吧然後那你告訴我這個b是啥b是不是咱自己的模型拿著它裏邊的768是不是處理成我們的結果呀是這個意思好了那行所以說a是Burnt模型b是咱們幹嘛呢把單獨的思想量抽取出來作為曲子的語氣息簡單來說就是把Burnt模型處理後的那個768為咱是不是給它映射成往這兒映射成幾為兩倍的動作就它好了那這個內容就先聊到這兒那最後總的回顧一下流程大家有沒有發現這個動作是咋做的切到這兒總的回顧一下Ctrl-Shift的相片上來以後導包不說了上來以後先幹分子器和模型這時候就叫切音學習啊那除了這兩步順暢跟咱自己做是不是一樣的第一步操作就是加載數據在這兒在這兒先加載數據再做數據的預處理拿到數據加載器有了數據加載器

## 16:48–17:46

這是第一動作那第一動作獲取到數據加載器有了加載器就可以做第二個動作第二動作是幹嘛的呢就是創建模型創建模型這是第二步其實你會發現一件事如果是在實際開發力塊做的一個事那工程有比較緊你如果確保你自己的這個模型是沒有問題的那下班是不是有個測試模型的幾個函數是不是有個這個代碼啊你可以刪掉就不用刪掉不用因為我確保我的模型沒了幾你測試都不用測你不用測試以後但是有兩個動作你得寫告訴我這兩個動作分別是什麼訓練和評估沒了這就是未來你寫代碼的流程它其實就是這樣做的咋做的呢再做一遍先拿到數據加載器讓把你的模型打好打好模型以後接起來訓練然後再做一個預測搞定聽懂了不後半壓力都是模型的加載來了以後先處理

## 17:46–18:06

處理完數據以後拿到加載器拿到加載器以後穿進模型穿進完模型以後訓練訓練完了以後預測後半壓力全是這個思路好這個內容就先聊到這那內容總結呢我們也就先總結到這裡大家需要注意一下這個內容就賠了好了來我把你的點保存一下
