# FastText文本分类原声逐段记录：P153

> 本文直接由视频音轨识别并按约一分钟分段，不使用站内字幕。
> 它用于核查老师讲解是否遗漏；口误、识别错误和重复表达会在对应学习文章中进一步校正。

## 00:00–00:59

接下来我们来讲讲最后一个语言就是这一块。优化思路我们可以来看,比如说第八个最后一个叫《保存和加载模型》加载模型,加载模型。啥意思呢?这是模型啊,如果你而向刚才那个字路挑三,你给它一个时间是一个小时,它跑了一个小时,终于把模型调的差不多了,结果你没保存,那你下次用,你是不是又得把那个小时模型再跑一遍啊?那这样做也不太好了。所以说,我要给大家讲一下保存和加载模型。来,咱们给大家讲讲啊,土渡酒,土渡酒,酒的范就是保存模型。保存模型,那么保存模型就行了。来吧,Defan的后边写上DM模,09后边不写上C模就OK了。好,先不知道地方还写啊,先写Past。Past完事以后,这里边我们先做个测试,来第九个。第九个操作教权的测试保存模型,

## 00:59–01:58

得09save。然后,因为你要保存到model里边,随时要在项目下新界的页面叫motl,叫model。那也就是说,在一会模型要保存到这里。好,那么接下来我们先来看它里边的第一个动作。它的第一个动作呢,就是之前一样,叫或许我待在写边啊,我就不复制粘贴了。或许,或许是什么?训练模型。好,其实就是有监督,有监督训练,有特征,有表现。好,那接下来呢,就是得到或许训练好的模型。那就是model等于,记得吗,Fast的Tex,然后DR,对了什么?Train SuperWise的里边写上Cook-up-re,叫Cooking-re-train。然后接下来后边写上一个,怎么?比如说训练30轨吧,30轨,然后训练率呢,我们调成1,好,二Grid模型的情况。

## 01:58–02:58

来,这几个参数应该不用解释吧,参一是什么?训练级的一个路径。参数是哪一个?训练,设数,参三,许一率,参四是我们的NGrid的特征。好,OK,就行了啦。那接下来我们做下个动作,第二个动作就是保存模型。这就上来保存了,对的,model.savemodel。你看,超级好玩,点杠model,保存到model下面的哪一个里边。Fast的Tex,下了线model。点model。那又是说老师,不对啊,这个数字名不应该是PTH或者说PKL或者啥的嘛,其实无所谓,都行。那保存完模型了,那一看,接下来我还能做一件事,第三个动作叫模型。加在,怎么加在?model2,你看,model这个地方是一个对象,对象创建完毕,可以保存下来。

## 02:58–03:52

model2,稍微把它加在下面就行了,怎么做?FastTex的点,load点当,Fastmodel把它加在下来。加在下来,能用不老师,能不能用,你别问我,你得问他。这下管怎么说了,是不是使用模型,接着运测呀,看到谁,来一餐,接下来我要做什么。最后一块你回过来以后怎么做,你后边这个屏幕这儿就可以不写了,就不写那么多了,就写一个吧。来四,使用模型,接着运测。运测就是参一,model点perdict,把这个扔进去,然后片子运算,threaten的,等于0.5乎。这边使用模型,接着运测,这个操作。哪里改成2?

## 03:54–04:46

哦,这样啊,model要改成2,为啥model要改成2呢?你要改model是不是还是请问那个,model2的话是不是说明才是你加在后的这个模型。能看明白吗?这个细节一定要注意啊,后边就是运测结果是谁谁,来,继续右键执行。好,稍等一下,稍等一下。好,来,model稍稍等一下。你看,这个结果是不告诉我们,是这个标杉,概率是多少,0.73,其实就是43%,73%,然后这里边我想说一句话,我想说啥呢?它这个结果,其实还挺高的,这个结果,然后我想说一句话,说啥呢?往这来看。

## 04:46–05:42

影三这里边确实发现,我是先把模型保存下来,所以在这里边的model,就突然有一个保存模型的文件,就是它在这,模型保存下来了。那你后边用的时候,是不是又把这个模型给它干嘛?加载出来了,就这,加载出来了,然后做一个具体的运测,所以这个细节呢,大家要简单的去注意一下这个事情。好,这是保存,是siva,那加载呢,是load,是这个操作。好,关于fastcast,它的一个调用,咱就暂时先说到这个地方。未来如果说你的知情确实很好实,那你就可以先准备一个什么操作。刚才我们说了,知情好好实,你就可以把模型先保存下来,是不是等它将来用的时候,直接加载就可以啊。注意一下细节啊。好了,这个内容就整个事,来总结一下啊,说一下文文分类的概念和文文分类的准备,来吧,大概聊一下。

## 05:46–06:40

啥人文文和类啊,给文文打个招,是不是就是分类啊。他刚才讲了有七种情况来着,文文分类是什么三种情况,第一种情况是什么?二选一。二分类问题吧,第二种是什么?单分类是二,多分类,还有种是多分类是二,多分类,然后一共三种情况,要注意。那多分类是二,多分类是三,我们是想做的。多分类是二,多分类是三,是想做的。多分类是二,多分类是三,是想做的。是不是转成多分类的问题,然后再来做处理啊?好,行,来吧,说一下Faststyle文本的一篇,告诉我刚才讲啥了。其实就有个Faststyle text,点儿,讲啥呢?

## 06:40–07:39

你看,Pretty给了预设,Test的测试,Sympmodel是保存文,然后呢,Low是加载的号呢,那加载的时候在这里边注意啊,Low的model,那如果说这里边加载出问题了或者怎么样,那你加载调整这里就行好。那再往下增加冷触,学习率,特征,损失函数,自动创造三,等等等等,这类,那这个传统其实就是我们要去玩的一个动作,要玩的一个动作。Faststyle的确认是什么什么等等等等,这就我再说了。好,这是它的文本,只如何做分类的,这里边有个题啊,这几个一篇,大家稍微记一下就行,来看这个题,我这儿特别提问。嗯,嗯。让它跑着吧,我要开始找这个题了,来。

## 07:48–08:47

在Faststyle之中,不管是想要训练模型和使用,Grid和Syn,应该是往哪个参数。那Grid,这不就数分的吗?是不是选择一样,第二题,如何将来可以影视的Faststyle模型化和Syn。孙保孙,这个同学啊,来,停,谁?别喊是我啊,切波。啊,水避还是水,要不就是Syn模的,要不就是Syn,摸一个吧。是哪一个?错了,手写啊。大家敢选择,A,上边这个选择,A,好,来,最后一个。多分类,都在Syn的数据中,是有想要哪个参数。

## 08:49–09:30

这不刚说过吗?是不是OVSA,谁念八号啊?这个单词不念八号啊,到OVA啊,不念八号啊。OVA,你别这么搞,我可没想念过。然后OVA,叫Van VSO,所以这个答应该是水额。这三个题,有没有发现一件事,这三题大家去选什么?我要不说话,很有同学,第二个或者第三个才会往外,就换一份,所以这个细节要注意啊。那到这儿,关于5G玩的第一棒Faststyle的应用,这就暂时先聊到这儿。
