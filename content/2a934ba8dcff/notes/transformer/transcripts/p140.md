# Transformer原声逐段记录：P140

> 本文直接由视频音轨识别并按约一分钟分段，不使用站内字幕。
> 它用于核查老师讲解是否遗漏；口误、识别错误和重复表达会在对应学习文章中进一步校正。

## 00:00–01:00

好,接下来我们把整个的回调这块儿,编码器和掩码器的地方,我们把这个动作去给它完成了。那我们一块儿来看一下,你该怎么去写啊。好,第一个编码的Encoder,Encoder,这个是一个编码的部分。我需要一个SourceX,需要一个SourceMask。那么这里边做的事情是什么呢?叫得到编码器的一个输出结果。然后接下来我们要做点什么器,叫编码器,它的一个叫形象、闯波、过程。那SourceX的身上呢,叫编码器的一个输入。编码器的输入它身上呢,是Bit Size,注意啊,Bit Size。还有一个操作就是我们那个叫Syphoslin,对应的就是我们那个24。那SourceMask就是你的输入训练的研码,输入训练的一个研码。研码呢,大家还记得输入训练的研码,那个研码呢,还记得我们把它分成了几个图嘛,分成了几个头啊。

## 01:00–01:56

所以这个操作就是叫En,看看这几个头,Syphoslin,然后还有一个叫Syphoslin。这个动作就是我们以前一直在说的那个844,844。然后这里边用的是哪个Mask呢,是PendingMask。这里边我们要写一下,叫PAID,Dig,PendingMask。PendingMask在这里边,它冲,PID,Dig。PendingMask在这里边,就是做填冲的意思,是防止填冲的一个操作,填冲的按的值,然后影响注意力,叫注意力,然后计算结果。好,那接下来Return,那你返回的值是什么呢?就是Incordial Autoport。Incordial Autoport是谁?Incordial Autoport就是这个紫色的位置,在这。好,那么这里边我们来看一下这个操作,它做的Mask是干嘛的呢?

## 01:56–02:52

是在这里边注意啊,它做Mask是为了更好的找值,它是为了研码,它是为了算出位地,这个也是。来,Incordial Autoport,这个是编码器的,编码器的一个输出。编码器的输入是24,那输出呢,这个结果就是一个,叫24512,你得写这个啊,叫中方号,Badge Size,然后Safe Slint,还有我叫Beam Model,它对应的维度就是一个24512。来吧,咱看看这里边这个内容应该怎么写啊,首先第一个动作应该是交对,SolidX是谁?SolidX是谁?SolidX就是这个Inputs在这,这个位置能够让我轻松播,在这对SolidX,所以第一步操作就是对Soil SE,SolidX叫进行磁线路,磁线路处理,然后转成,转成,销量。

## 02:52–03:52

好,那来吧,那怎么做呀,EMBD,你摆的X等于三毫点SolidX你摆的,把它扔进去。那第二个操作就是通过编码器,编码器处理,通过编码器处理,处理什么呢?处理切入后,切入后的一个数据。所以这个数据是谁呢?Return,Self,DR,DR谁呢?IncelDR,你扣点就可以了,把谁扔进去啊,把你的InputsX,还有SauceMask扔进去,就是你的这个SauceX是谁呢?经过Inbd处理以后,你的InbdX就是经过为编码,还有你的一个切入处理之后,这个绿色的点,叫InbdX去它。InbdX跟谁呢?跟SauceMask,就是你注意一些字,在这里面的研码,经过把这两个参数传进去,那我的编码器就可以做处理了。Return,反过来的这个结果,其实就是这个紫色的这个点,还能更唱我思路吧,这是你的编码器。

## 03:52–04:48

那么接下来我们来看看结码器,它是怎么去做的。结码器呢,也是一样类似的套路,把这个操作给它折叠一下。折叠以后,Defand这里边写一下,Decode,好,那么你也可以叫它Decode,这个叫Decode,上把这个操作就把R去掉,编码、结码、动作,叫Decode。Self,它要的是谁呢?它要的动作比刚才那个就要多了。我要一个pr,get, target,目标by,我要一个incoder,results,就是incoder的一个output。output,我还要一个操作什么呢?sauce master,我还要一个target master。来,开始了,跟着我的思路。首先这个操作是得到,得到,得到谁?得到结码器的一个输出结果。

## 04:48–05:47

好,来,开始,记什么?结码器的一个什么?显向、传播、过程。targetby刚做的是什么?结码器的一个输入。结码器的输入在这里边,例如谁呢?叫跟上班一样,24。所以我想说的是targetby是结码器的输入,就是它这里边的输入。左边这个操作叫saucex,看下啊,左边这个操作叫saucex在这儿,24。右边叫targetby,它也是24,因为这是输出的,所以都是一样的。还有能跟上来吗?接下来继续啊,再来,incoder的output,这个是什么?这个是叫编码器的输出结果,叫谁?24、512。那这个是谁?incoderoutput的事情告诉我。这是这个紫色的点,就这儿,它能看懂不?好,再来,这个saucemask是干啥的?saucemask刚才说了,代表用的是什么mask?

## 05:47–06:44

pddig、panning,哎呦,你又开始了。pddig,代表panningmask,panningmask是干啥事的?告诉我。防止填充,填充的,派的值,然后干嘛?影响,注意力,计算结果。好,所以就是panningmask是填充的。target这个操作是什么?叫sencentencemask,centencemask。那这个操作写下,叫代表,那虽然都是研码,但是你研码也不一样。好,那这个是干啥的?防止未来的信息,然后被提前,提前是什么?提前利用。说白了就是防止偷看,偷看未来的词,未来的词,所以说我要做个研码,那注意啊,targetmask是给谁用的?targetmask是给下半点用的,就下半点个自主为地。

## 06:44–07:40

然后这样的话,你的二次,我要给你遮掩起来,比如说你预测的是第一个句子的第一个词,那好,那我啥子啊,你看不见。你要预测第一个句子的第三个词,那你只能看到第一个句子的前两个词,第三个第四看不见,是通过这个targetmask,是通过它来研码的,而你算注意力是通过这个saucemask来做一个注意力的计算。能跟上来吗?倒这了,它是这几个材料在这,你一定要对着这个图去看,不看的话看不懂的,Ruthen,解码记得一个输出结果,这个不用说了,跟那台一样,24512。来,说说解码记怎么玩的。一,叫对target的Y,然后进行一个磁芯肉处理,转成像量,是不是隐掰的Y等于Saucemask或者target的隐掰等于它。

## 07:40–08:38

第二个动作就是,干嘛呀,解码记处理芯肉获得数据,Ruthen做个返回,三户者Decoder就行了,把谁呢?隐掰等于by,以coder.outputsaucemask,targetmask,也就是说,你有没有发现一件事,你未来只需要调我就可以了,为啥呢?因为在我这里边,我已经把编码和解码给你服装好,而不是像之前的,哎呦,咱还得在这儿use generator一下,之output,Decoder呢,气势里边,哎呦,咱还得在这里边,use Decoder,咱还得在这里边去玩一下,怎么怎么样,不需要。而现在这个活就是,我已经封装到,这个架构就是我的transformer,我就已经封装好了。看明白了吗?到这儿就封装好了,那我的transformer有啥?你告诉我,我的transformer里边,我有编码动作,我是不是还有解码动作呀?那其实原因就在这儿,好了,那这个动作封装完毕以后,咱是不是该测试了?

## 08:38–08:42

好,那我把这个动作去先保存一下啊。
